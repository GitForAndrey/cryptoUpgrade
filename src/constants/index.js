import { COLORS, SIZES, FONTS, GLOB_STYLE } from './theme';
import { filtersMarketCoins, filtersCoinValue } from './data';

export {
  filtersMarketCoins,
  filtersCoinValue,
  COLORS,
  SIZES,
  FONTS,
  GLOB_STYLE,
};
