import theme, { COLORS, SIZES, FONTS } from './theme';
import {
  filtersMarketCoins,
  filtersCoinValue,
  coinsValue,
  assetsValue,
} from './data';

export {
  filtersMarketCoins,
  filtersCoinValue,
  assetsValue,
  coinsValue,
  theme,
  COLORS,
  SIZES,
  FONTS,
};
